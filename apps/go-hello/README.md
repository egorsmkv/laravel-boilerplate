# Go sample application

### Usage

```bash
# Build image
just build

# Linting
just lint

# Formatting
just fmt
```

### Development

Install required packages on Ubuntu:

```shell
sudo apt-get install libzmq3-dev
```

Build:

```shell
go build .
```
